# Runtime dependencies for app.py (Streamlit Community Cloud)
# scikit-learn version must match the version that created the .pkl files,
# or unpickling will raise InconsistentVersionWarning / fail silently.
# app.py uses the stdlib 'html.parser' backend, so lxml is not needed.
streamlit==1.29.0
scikit-learn==1.3.2
numpy==1.24.3
scipy==1.15.3
nltk==3.8.1
beautifulsoup4==4.12.2
